# Dependencias
node_modules/

# Variables de entorno — nunca subir al repo
.env
.env.local
.env.*.local

# Build del frontend — incluido en el repo para que Railway lo sirva
# frontend/dist/

# Logs
*.log
logs/

# Datos del negocio — van en el VPS, no en el repo
# Comenta esta línea si quieres hacer backup de los datos en git
api/src/data/

# Respaldos y scripts de migración (no subir al repo)
api/src/data_BACKUP_*/
api/migrate.js

# Sistema operativo
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
