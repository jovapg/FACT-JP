{
  "name": "facjp",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/server.js",
    "install-deps": "cd api && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
